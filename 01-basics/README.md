# JavaScript Basics

## Topics Covered

- Variables (`let` and `const`)
- Primitive Data Types
- Pass by Value
- Pass by Reference
- Equality Operators (`==` and `===`)
- Conditional Statements (`if...else`)
- `switch` Statement

## What I Learned

- Difference between `let` and `const`.
- How primitive values are copied by value.
- How arrays are copied by reference.
- Why `===` is preferred over `==`.
- How to use `if...else` for decision making.
- How `switch` works with multiple cases.

